# Agent Note: YourBuddy product workbench distribution

Status: implemented

English | [中文](2026-08-22-yourbuddy-product-workbench.zh.md)

## Problem

Installing a domain plugin after installing a generic Harness desktop still asks the user to understand profiles, package registries, Python environments, Skills, and adapter paths. The Harbor integration also spans a Cordis bundle and a Python runtime, so distributing only its npm package leaves a partially configured product. Reusing `~/.dsh` lowers migration cost for the generic desktop but lets a branded workbench modify another Harness installation's profiles and sessions.

## Decision

The first-party Personal Workbench compiles against the same DSH release as the product. When DSH removes an aggregate Client package, the plugin moves its imports, Client injections, TypeScript references, and peer declarations to the owning split packages; release preparation must prove the resulting Host, Client, and offline install together.

YourBuddy is a product fork of the Sakana Tauri distribution with the upstream Git history retained. The reusable desktop supervision, first-run Harness provisioning, notification overlay, and updater remain in `apps/desktop-tauri`; product assembly stays in that subtree and does not change the Agent Loop or Service packages.

The committed `apps/desktop-tauri/product/harbor-evolution` snapshot contains the reviewed Harbor Cordis plugin and its `evolve-agent-with-harbor` Skill. The sibling `product/harbor-python` snapshot contains the matching Python adapter from the same stable GitHub Release. `product/dsh-codex-auth`, `product/dsh-better-sidebar`, and `product/plugin-marketplace` contain reviewed npm artifacts, `product/context-doctor` contains the reviewed package selected from its GitHub `main` head, and `product/personal-workbench` contains the first-party branding plugin. Every externally refreshed snapshot preserves its upstream license and records its exact version or commit, immutable archive source, integrity value, committed tree hash, and local changes in `YOURBUDDY_UPSTREAM.json`; the generated `.bundle-manifest.json` records selected package versions and the whole-bundle hash consumed by the application. Peer-metadata corrections are allowed only for exact upstream versions declared in `product/plugin-update-policy.json`; a functional compatibility patch remains explicit in provenance and must retain assembled release-smoke coverage.

`bundle-harness-source.mjs` copies all six product Cordis packages into the trimmed workspace and adds them plus the in-tree `@deepseek-ai/dsh-subagent-codex` package to the bundled CLI dependency closure. The generated bundle manifest records the Codex provider version and YourBuddy's default Agent Preset separately from externally refreshed product snapshots. The generator derives the system `codex` Preset from the shipped Standard composition, publishes it under the display name `Codex`, and enables only its existing `subagent_codex` row; Standard and the other shipped Presets remain unchanged. The desktop overlay selects that Preset as the base default, selects the Codex search provider, configures an existing Harbor Bundle row when present, and inserts uniquely named fallback rows for the Codex subagent provider, Codex Auth, Search, Image, Better Sidebar, Context Doctor, Plugin Marketplace, Personal Workbench, and Harbor Evolution. Each fallback disables itself when another enabled row already mounts the same package entry, so a standard Profile Bundle installed in the isolated YourBuddy Profile does not collide with the product overlay. A fresh Profile and a new Session with no explicit selection therefore expose `subagent_codex`; an explicit user default remains authoritative, and an existing Session retains its recorded composition. On upgrade, native Profile repair rebinds only YourBuddy-managed `link:` dependencies that point into an older content-addressed Harness tree, then runs the standard Profile install so its lockfile and symlinks follow the active tree. Registry packages and local links outside the application's `harness-versions` directory remain untouched. First launch therefore resolves committed plugin code rather than installing changing `latest` packages. A committed product lockfile fixes the remaining production graph; the release build fetches it into a checksum-bound compressed Store, and the product-managed pnpm reconstructs `node_modules` with frozen, offline semantics.

Local release preparation is the only latest-channel refresh path and is invoked explicitly with `pnpm --dir apps/desktop-tauri run prepare:release`. It queries the latest stable npm artifacts for Codex Auth, Better Sidebar, and Plugin Marketplace, the latest stable Harbor GitHub Release for the Cordis and Python pair, and the Context Doctor `main` head; first-party Personal Workbench remains outside the updater. The command refuses dirty managed product paths by default, with `-- --allow-dirty` as an explicit developer escape. It does not select an older fallback when the latest candidate is incompatible.

The General settings page exposes Application lifecycle and Network proxy cards. Check and update requests the existing signed Tauri update flow; Restart YourBuddy stops the private Host and relaunches the complete application so the next boot scans packages installed through Plugin Marketplace. Network proxy selects the application-wide routing policy described in [YourBuddy application-wide network proxy](2026-08-30-yourbuddy-global-network-proxy.md). The loopback Client sends fixed versioned messages without an update URL, target version, or command name. The Tauri shell accepts them only from the active workbench iframe at the exact Host origin and maps each action to a literal allowlisted command. Marketplace repository and npm links use a separate fixed message and command; both the shell and Rust command accept only an HTTPS GitHub repository, npm search, or npm package page before opening the system browser. These end-user paths remain separate from local release preparation: an application update installs a signed YourBuddy release whose committed product snapshots already passed compatibility checks, rather than refreshing plugin source on the user's machine.

The refresh validates every policy path before reading or replacing a destination: product destinations stay below `product/`, archive source paths stay below the extracted checkout, and managed destinations are unique and non-overlapping. It downloads each archive over HTTPS with request, compressed and expanded byte, entry-count, extraction-path, and file-type checks; npm artifacts must match registry SRI, and every archive receives digest-bound provenance. It builds the prospective snapshots in a temporary staging tree, applies only exact-version peer overrides automatically, and validates package identity, managed Node support, DSH peer ranges, the bundle patch, built Host and Client entries, Client injections, license presence, and the final tree hash. Reviewed functional patches remain committed snapshot changes named in provenance; a newer upstream candidate that omits one must independently satisfy the same release smoke. No destination changes until every external candidate passes, and any subsequent release-preparation failure restores all managed snapshots and the product lockfile. After replacement, the command rebuilds Harness from the same explicit Client environment as tagged CI, regenerates the frozen product lockfile, and proves that each DSH/Cordis peer in the lock and installed graph links to the bundled workspace rather than installing a second runtime. It then creates the digest-bound offline Store and performs the actual frozen production offline install. Release acceptance and native first launch disable pnpm's automatic version switch and trust the committed lockfile because YourBuddy separately pins the pnpm archive and verifies the Store and bundle digests. The Harbor commands and assembled Host run with a credential-scrubbed environment and temporary home, DSH, and temporary directories. Headless Chromium must receive every product Client bundle, activate the complete Client Loader graph without page or console errors, mount the workbench, show Codex as the default Agent Preset, reject Marketplace one-click installation when a repository has only a non-DSH package, select a differently named scoped DSH Bundle while ignoring a sibling SDK package, invoke only the restricted repository, npm external-link, network-settings, signed-update, and application-restart commands, enable installation confirmation, render a package-specific persisted failure, exercise the Network proxy test and save-and-restart path, expose both Application lifecycle actions, and pass the Context Doctor API check; the Host must also shut down normally. A successful run leaves the snapshots and lockfile for review and commit.

`prepare-yourbuddy-runtime.mjs` builds a macOS arm64 resource with portable CPython 3.12, the committed Harbor Python adapter source, and Harbor. The Harbor Cordis and Python package versions must match. The generated virtual environment uses only relative interpreter links, runs directly from application resources, and is invalidated by a source-tree digest. The script accepts `YOURBUDDY_HARBOR_PYTHON_SOURCE` as a temporary override. `sync-product-plugin.mjs` refreshes both committed snapshots from an extracted Harbor Release or source checkout through explicit allowlists, including the exported Harbor result schemas.

The native Host always uses `YourBuddy/dsh-home` under platform application data and creates `YourBuddy/workspace/jobs`; it does not adopt or import `DSH_HOME` or `~/.dsh`. This product override supersedes home adoption in [desktop host environment and home adoption](2026-08-14-desktop-host-env-and-home-adoption.md) for YourBuddy's native launch while retaining native host Node discovery and replacing global pnpm reuse with a product-managed copy. The product overlay extends [desktop shell overlay plugins](../architecture/2026-08-14-desktop-shell-overlay-plugins.md) with the required product rows; a missing Harbor runtime is a boot failure, while notification delivery remains optional. Codex Auth reads the official Codex CLI's Host-side login state and never moves tokens into browser settings. The Codex subagent uses that native product state through the package-local official runtime; availability does not copy authentication into the provider or start a process. The [Job-scoped Host model bridge](../architecture/2026-08-23-harbor-host-model-bridge.md) freezes the current Agent model for Harbor Candidates and passes only a short-lived capability into the task environment, so GPT Auth remains the default Candidate model path without exporting its OAuth credential. Better Sidebar's reviewed `node-pty` build is covered by the existing workspace `allowBuilds` policy.

The native provisioner reuses Node only when its version and `process.platform:process.arch` match the product target. It never adopts a global pnpm; it provisions the pinned product copy so wrappers and native packages installed by another Node architecture cannot enter the first-launch dependency graph. macOS arm64 Node and pnpm archives are fixed by digest in source. The Store archive digest is recorded in the generated bundle manifest, verified before extraction, and removed from the writable Harness tree together with the expanded Store after a successful install. The 35,000-file Store is compressed into one resource so Tauri does not package a large mutable-looking tree or follow platform-specific links.

Private `dsh`, Node, and pnpm shims are prepended only to the Host process tree. The product does not persist a global `dsh` command or edit a shell profile by default; `YOURBUDDY_PERSIST_DSH_CLI=1` is an explicit developer-only opt-in.

The desktop overlay also contributes a workbench Content Security Policy and a no-referrer policy through `webserver/index-inject`; the Tauri-owned splash and shell have their own CSP. Cordis client execution requires `unsafe-eval`, but object embedding and base-URL mutation are disabled.

The release target is `aarch64-apple-darwin` only. An `yourbuddy-v*` tag consumes only the committed product snapshots and frozen lockfile; tagged CI and ordinary desktop builds do not query update channels. The workflow builds and signs the App/updater once, bundles the DMG from that App, validates the extracted updater runtime, and publishes the arm64 asset set directly from the macOS build job. This product-specific release set supersedes the platform matrix in [cross-platform desktop source provisioning](2026-08-14-cross-platform-desktop-source-provisioning.md); the underlying provisioning code remains available for future targets. Apple code signing and notarization remain separate credentials from the YourBuddy-specific Tauri updater signature.

## Alternatives considered

**Distribute only the plugin.** Rejected because users would still install and reconcile the npm bundle, Skill, Python Adapter, interpreter, and paths, which is the product burden this distribution removes.

**Build a new desktop shell.** Rejected because the Sakana shell already owns first-run provisioning, process-tree teardown, startup recovery, tray behavior, notifications, and signed updates. Reimplementation would create a second lifecycle system without adding product value.

**Download components or production dependencies on first launch.** Rejected because it makes product availability depend on npm, PyPI, a Node mirror, `uv`, and interpreter installation after the user has already downloaded the application. YourBuddy carries both domain capability and the remaining production dependency graph as fixed installer resources.

**Enable the Codex delegation tool in every shipped Agent Preset.** Rejected because an explicit choice of Standard, Code, Minimal, or Creator mode must retain that Preset's declared model-visible tools. YourBuddy instead adds one Codex Preset, makes it the product default, and leaves the other compositions unchanged.

**Refresh latest plugins inside the tagged build.** Rejected because the same tag could select different source archives over time or fail after an upstream channel changes. Network refresh is an explicit local preparation step whose reviewed results become committed release inputs.

**Adopt the user's existing Harness home.** Rejected because a product application must not silently add its bundle or configuration to a separately managed Harness environment. Users can export data explicitly when a migration workflow exists.

**Keep the full upstream platform matrix.** Rejected for the first product release because the Harbor runtime resource is platform-specific and the requested delivery target is macOS arm64. Additional targets require their own portable runtime and acceptance path.

## Consequences

Installing one DMG yields a named AI workbench with an aligned Harbor plugin, Skill, Python adapter, Codex Provider, default Codex Agent Preset, Codex Auth, Better Sidebar, Context Doctor, Plugin Marketplace, and Personal Workbench already mounted. Developers refresh external product snapshots explicitly, review compatibility failures at preparation time, and reproduce a tagged release from committed plugin code, a frozen lockfile, and digest-checked build inputs. Exact external component revisions remain in their provenance and bundle manifests instead of release-process prose. Plugin Marketplace treats public GitHub topic membership as discovery metadata, enables confirmation only after npm links a package with `dsh.bundle.patch` to that repository through its Repository field or owner-scoped DSH upstream metadata, resolves the package's actual npm name while excluding sibling SDK and CLI packages, and keeps each install result associated with that package. A confirmed result is installed into the isolated Web Profile through `dsh plugin add`; installing it grants the package the same Host privileges as any manually installed DSH plugin and requires network access at the time of use. Repository and npm links open in the system browser only after the desktop validators accept their restricted destinations. The macOS arm64 release gate verifies the desktop version sources, builds the Harness, App, DMG, and updater archive, checks the DSH parser plus native and WSL Host launch vectors, relocates the portable Runtime away from its build path, and runs both Harbor entry points before checksumming and publishing. The installer is larger because it carries CPython, Harbor, the official Codex wrapper and platform payload, Codex Auth, Better Sidebar, Context Doctor, Plugin Marketplace, and the compressed Node dependency graph; Harbor Jobs still require Docker, Codex delegation requires valid native login state and remains absent when the user selects another Preset, Codex Auth remains an unofficial local single-user integration, the model bridge depends on live Docker-to-Host reachability, and an ad-hoc-signed, unnotarized DMG requires manual Gatekeeper approval until Apple release credentials are configured.
