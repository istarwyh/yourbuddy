# Agent Note: Desktop host toolchain scan and home adoption

Status: implemented

English | [中文](2026-08-14-desktop-host-env-and-home-adoption.zh.md)

## Problem

The desktop installer isolates `DSH_HOME` under application data and downloads Node even when the machine already has a compatible toolchain or a CLI/Web Harness home. Users who already ran `dsh web` or `dsh` lose their sessions and API keys on first desktop launch, and first launch repeats a multi-minute runtime fetch.

## Decision

**Provisioning scans the host before any mirror download.** A ready runtime manifest that still names a usable Node and pnpm — the private runtime or the host binaries recorded at provision — skips the scan, the harness seed, and `pnpm install` ([repeat-boot skip](../bug-fix/2026-08-17-desktop-repeat-boot-host-toolchain.md)). Otherwise the shell probes the private runtime, the process `PATH`, and well-known Node/pnpm locations. On Windows it also reads the durable user and machine Path (and `DSH_HOME`) from the environment store once per process, because a Start Menu launch often inherits a stale GUI process PATH. A Node that satisfies `^22.19 || >=24` is reused for Host startup and `pnpm install`. A usable pnpm is reused the same way, including a Corepack shim beside the selected Node. The private Node archive is fetched only when the scan finds no compatible Node; pnpm is installed through the selected Node only when no usable pnpm exists. This extends [cross-platform desktop source provisioning](2026-08-14-cross-platform-desktop-source-provisioning.md) without changing the fallback download.

**The Host adopts an existing Harness home instead of always using `dsh-home/`.** Selection order is `$DSH_HOME` when that directory already holds Harness data, then `~/.dsh`, then the isolated application-data home. A directory counts as a Harness home when it contains `sessions`, `.credentials.yaml`, `.env`, `profiles`, or `settings.yaml` / `.yml` / `.json`. Missing sessions, credentials, settings, and other ordinary files from every other discovered home are copied into the selected home; existing files are left in place. `desktop-overlay` and `node_modules` are never imported — the overlay is regenerated by the shell. Adoption deletes `profiles/node_modules` in every discovered home, and deletes `profiles/*/node_modules` only when the install is broken: a dependency the profile's `package.json` declares cannot be resolved to a package directory under its `node_modules` (a missing manifest or a dangling junction, since resolution follows reparse points), or the profile has `node_modules` without a readable manifest. Removal unlinks reparse points without following them into npm-cache or a harness tree; intact installs are kept, so routine Host restarts do not force profiles back through `dsh plugin install`. Reparse points and per-file copy failures are logged and skipped; they do not abort boot. Access-denied, missing-path, and file-in-use errors (`os error 5` / `3` / `32`) during home adopt, seed, PATH-bridge writes, or overlay copy are logged with the path and fall back: isolated home, an existing `harness-versions` tree, the current process PATH, or Host without overlay. Before the Host starts, every profile whose declared dependencies cannot be resolved runs `node …/pnpm.cjs install` in that profile directory when the entry exists, otherwise `dsh plugin --profile <name> install` on the bridged PATH (10-minute cap, no console window, output tail on failure, re-verified after pnpm reports success); only the `web` profile failing that repair aborts boot, other profiles defer to a later `dsh plugin`. The notify endpoint is the same kind of degradable step: a failed bind skips the overlay instead of aborting boot. A Host that dies naming a loader entry (`failed to apply loader entry <id>`) is respawned with that plugin disabled through a per-session rescue `--patch` overlay (up to four plugins per boot), so one broken community plugin cannot keep the desktop closed; the disable is not persisted, so a fixed or updated plugin loads again on the next restart, and the bypassed ids surface through a system toast and the boot log.

[YourHarness product workbench distribution](2026-08-22-yourharness-product-workbench.md) always selects the product's isolated home, restricts host Node reuse to the native architecture, and replaces global pnpm reuse with a product-managed copy while retaining profile repair.

## Alternatives considered

**Keep the isolated `dsh-home/` and copy CLI data into it.** Rejected because later CLI or `dsh web` launches would not see desktop sessions, and keys would drift between two homes.

**Always set `DSH_HOME` to `~/.dsh`, even when that directory is empty.** Rejected because a fresh desktop-only user would then write into the default CLI home without having opted into a shared tree.

**Use any Node on `PATH` without an engine check.** Rejected because Node 18/20 and Node 22 below 22.19 fail the workspace engines range and would break Host startup.

**Merge credential YAML documents key-by-key.** Rejected because a structural merge can corrupt the managed store. Missing-file copy keeps CLI keys when both homes already have `.credentials.yaml`, and copies the desktop file when the selected home has none.

## Consequences

A machine with a compatible Node skips the runtime archive download; `pnpm install` of the bundled tree still runs when that tree has no `node_modules`. A later launch that still has that tree and the recorded binaries skips seed and install. Desktop and CLI share one home when `~/.dsh` already exists, including sessions and keys. The overlay plugin is written into the selected home. A host Node that later disappears fails the next boot's skip and falls back to scan, then to the private runtime. The selected binaries are then exposed on PATH by [the desktop PATH bridge](2026-08-14-desktop-path-bridge.md).
