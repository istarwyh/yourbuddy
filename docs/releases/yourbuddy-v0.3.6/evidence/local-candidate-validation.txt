YourBuddy 0.3.6 local candidate validation
==========================================

Evidence date: 2026-09-09, Asia/Shanghai (UTC+08:00)
Issue correction commit: ae00df24539f07479a6d097cf0c06e86b493d86e
Merged correction commit: d0b55a7fee27fc240b1ef68ad14d968d4b96c0db
Baseline before release versioning: origin/master at d0b55a7fee27fc240b1ef68ad14d968d4b96c0db
Environment: macOS 15.6.1 (24G90), arm64; Node 22.19.0 for the bundled-Node path; Node 22.22.2 for the final local assembly and terminal tests; pnpm 11.7.0; rustc/cargo 1.98.0
Data and services: locally generated private CA, leaf certificate, HTTPS origin, and HTTP CONNECT proxy; no credential, account data, real enterprise certificate, or external model request

Passing observations
--------------------

1. pnpm --dir apps/desktop-tauri run test:personal-workbench
   Result: exit 0; eight files and 42 tests passed, followed by the production build.

2. pnpm --dir apps/desktop-tauri run test:product-refresh
   Result: exit 0; 43 product tests passed, including the updated network settings bridge and assembled product smoke.

3. Two independent concurrent invocations of node --test apps/desktop-tauri/scripts/node-extra-ca.test.mjs
   Environment: YOURBUDDY_NODE_BINARY selected the extracted bundled Node 22.19.0 runtime.
   Result: both invocations exited 0. Each started a local private-CA HTTPS endpoint and local HTTP CONNECT proxy, proved that the actual CLI Host failed without the CA, and received HTTP 204 with the CA.

4. cargo test --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml network_proxy::tests::
   Result: exit 0; 12 tests passed.

5. Focused WSL launch and provisioning tests
   Result: exit 0; five WSL launch tests and one WSL provisioning test passed.

6. Focused plugin catalog tests
   Result: exit 0; three tests passed.

7. GITHUB_TOKEN supplied only to the child process: pnpm --dir apps/desktop-tauri run sync:dsh -- --dry-run
   Result: exit 0; upstream DSH selection and desktop product inputs were current.

8. GITHUB_TOKEN supplied only to the child process: pnpm --dir apps/desktop-tauri run prepare:release -- --allow-dirty
   Result: exit 0 after the peer-policy correction described below.
   Client build: 220 artifacts.
   Harness source SHA-256: 5c765be554a75a8a3810281e8364d21b11792e83eb443366c05e10744794aed0.
   Runtime peer links: 54.
   Offline store: 38,708 files; SHA-256 c4c733da80b6027aa6cd946b7a047db98338d82625cee72b72ad928e5379fe09.
   Offline-store archive SHA-256: 262405620e237043ad157e66f5a95b199920686df6f78bb244e11409b21d77e8.
   Harness bundle SHA-256: 078ca9f07b7f46e8a5160bb7bafe2c5a41a8d8c86cb7f7fd376684cf7930f78e.
   Offline reinstall: 587 packages reused, zero downloaded, and 587 installed.
   Bundled Python: 3.12.14.
   Bundled Harbor: 0.21.0; Harbor DSH Evolution adapter: 0.9.5.
   Product smoke: all six assembled plugins passed.

9. pnpm run test:docs
   Result: exit 0; 15 tests passed.

10. pnpm run doc-sync
    Result: exit 0; 32 documentation gates passed.

11. pnpm run lint; git diff --check; focused rustfmt --check
    Result: exit 0.

12. pnpm run typecheck
    Result: exit 0 in the pre-push hook for the issue correction.

13. Pull-request CI run 34361867650
    Commit: ae00df24539f07479a6d097cf0c06e86b493d86e.
    Window: 2026-09-09 22:10-22:39 UTC+08:00.
    Result: success; all required jobs passed, including Linux and Windows exhaustive coverage, snapshots and artifacts, Windows native/build/Wine/observational lanes, Node compatibility, Python SDK, and the release-shaped Python runtime matrix.
    Merge: PR 19 merged as d0b55a7fee27fc240b1ef68ad14d968d4b96c0db at 2026-09-09 22:41:45 UTC+08:00 and closed Issue 18.
    Non-result: Cloudflare Pages preview run 34361867354 remained queued for the unavailable dsh-ubuntu-24-04-16core self-hosted label and was cancelled. It did not run and is not counted as passing.

14. pnpm exec vitest run packages/terminal/terminal-bash/tests/session.spec.ts
    Result: exit 0; 51 tests passed, including the shell-group delayed-output regression and the retained changed-foreground-group exact-readiness case.

15. pnpm exec vitest run packages/terminal/terminal-bash/tests
    Result: exit 0; five files passed, with 89 tests passed and three optional real-pwsh tests skipped because the faulty local PowerShell executable was deliberately absent from PATH.

16. External product snapshots and latest-channel dry-run
    Result: all six external product snapshot hashes matched their committed content; refresh:product-plugins --dry-run --allow-dirty reported every external product snapshot current and passing static checks.

Failures, negative controls, and recovery
----------------------------------------

- The first complete prepare:release attempt rejected Harbor Evolution 0.9.5 because npm prerelease peer matching does not accept the current DSH 0.1.2-rc.1 through the ordinary semver range. The script rolled its transaction back. Exact Harbor 0.9.5 peer overrides were added for the bundled DSH version, the obsolete client-runtime injection was removed, and the complete prepare:release transaction then passed. No partially assembled tree supports the release claim.
- The first pull-request CI run 34356938688 failed only in Windows coverage after session-projection-cache read durable sequence 2 while the expected sequence 4 fire-and-forget write was still pending. The affected product path was unchanged and byte-identical to recent successful Windows runs. The test now subscribes to the exact domain/changed event emitted after backend durability before reading the file. The focused file passed 21 tests once and then passed four additional independent sequential invocations. Final CI run 34361867650 passed Windows coverage. The failed run remains negative evidence and is not described as a passing retry.
- Release PR run 34367447468 failed one Linux coverage assertion after 16,461 tests passed: persistent PowerShell state reached scrollback but the second send settled with an empty viewport. Windows coverage and every other completed job passed. The terminal source and failing test were unchanged from run 34361867650. The backend had accepted the shell process group's stdin wait before node-pty delivered the command's final output callback; exact stdin-wait readiness is now limited to a different foreground child process group, while the shell requires its owned prompt or a bounded fallback. The focused 51-case suite and complete 89-passing-test package suite passed locally. Replacement CI remains pending and is not described as passing.
- The first final-candidate assembly rejected Harbor JavaScript and Python snapshots whose reviewed bilingual README synchronization had not re-recorded their tree hashes. The current content was compared against git history, only the two documentation projections had changed since the snapshot refresh, and all other external snapshots matched. The two exact current hashes were recorded, all six snapshots and the latest-channel dry-run passed, and complete preparation then passed.
- One final-candidate preparation attempt used a deliberately narrowed PATH that omitted the required uv executable and rolled back after the Node offline Store was built. The subsequent attempt restored the existing uv 0.12.5 tool path and completed the entire transaction; no output from the rolled-back attempt supports the passing claim.
- The real Host CA test originally used timeout-driven child cleanup that could reject before the child close event and leave servers open. Cleanup now rejects only after child close, awaits both server closes, and removes the temporary directory even when close reports an error. Two concurrent complete invocations passed.
- The first offline-store fill needed one external network retry. The completed transaction's zero-download reinstall and all product smokes passed; the interrupted observation is not used as product evidence.
- Eight optional local PowerShell probe processes entered a macOS uninterruptible exiting state during mistaken Vitest attempts. TERM and KILL could not reap them. Subsequent tests selected a PATH without that faulty optional executable and passed. This host-tool residue did not modify the repository or product candidate.

Evidence boundaries
-------------------

This record proves the source behavior, synthetic private-CA path through the actual CLI Host, native policy tests, terminal-readiness correction under deterministic local tests, product composition, complete local release preparation, and the earlier issue-fix pull-request CI. It does not prove replacement CI for the release correction, public product bytes, updater signature or installation, DMG identity, native App startup, packaged-WebView interaction, Apple signing or notarization, the deployed website, a real enterprise proxy or certificate, Intel macOS, or Windows and Linux desktop products. Those claims remain pending or unverified until their dedicated release stages complete.
