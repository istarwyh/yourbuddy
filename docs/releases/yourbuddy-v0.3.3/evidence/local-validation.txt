YourBuddy 0.3.3 local validation record
Date: 2026-09-06 21:30-2026-09-07 00:34 UTC+08:00 (Asia/Shanghai)
Release: intended yourbuddy-v0.3.3
Fix commit: be8abfaedeb25f01632f1d50727be54e69d26d7e
Version-preparation commit: 07b2440f5dd4b3c8fb5c02d44d4c52ed983c54a0
Release-blocker fix commit: 6b47dfd95ebd852d922fb5f915a3b00bf1b05aff
Carrier and PowerShell fixture fix commit: 7fbbd8223ad2225a27ba029d982dbf37cce4acbe
Environment: macOS 15.6.1 (24G90) arm64; Node 22.22.2; pnpm 11.7.0; Rust 1.98.0
Data: synthetic temporary workspaces and isolated application data
Model/service: local dsh web Host and controlled browser only; no real model provider

Installed application diagnosis

- Public /Applications/YourBuddy.app 0.3.2 reproduced: dsh web authentication required; reopen the URL printed by dsh web.
- Native boot.log still reported authenticated readiness passed and boot complete.
- A localhost-parent negative control reproduced HTTP 401 while separate 127.0.0.1 ports passed with the strict cookie.
- A release-mode arm64 binary with Tauri custom-protocol support was placed in a copy of the public 0.3.2 resources and ad-hoc signed.
- The copied app used an isolated YOURBUDDY_APP_DATA_DIR, reported authenticated readiness and boot complete, and rendered the visible workspace in the native macOS WebView.
- The temporary application, isolated data, and temporary screenshot were removed after inspection. No credential-bearing artifact was retained.
- An intermediate SameSite=None; Secure cookie-copy trial still failed in macOS WebKit and was replaced by the same-site loopback shell design.

Focused fix checks

- cargo test --locked desktop_shell::tests
  Result: 2 passed, 0 failed.
- cargo test --locked chrome::tests
  Result: 4 passed, 0 failed.
- cargo test --locked runtime::supervisor::tests
  Result: 13 passed, 0 failed.
- pnpm --dir apps/desktop-tauri run test:product-refresh
  Result: 41 passed, 0 failed.
- pnpm run test:docs
  Result: 15 passed, 0 failed, 0 skipped.
- pnpm run doc-sync
  Result: 32 passed, 0 failed, 0 skipped.
- pnpm run lint
  Result: exit 0.
- HUGO_BIN=/private/tmp/yourbuddy-hugo-0.165.0/expanded/Payload/hugo PRODUCT_SITE_BASE_URL=https://istarwyh.github.io/yourbuddy/ pnpm website:check
  Result: 70 projection tests passed; Hugo built 59 Chinese and 57 English pages; 57 product HTML pages passed local link, asset, fragment, and source-action checks.
- git diff --check
  Result: exit 0.

Website check recovery

- The first website check passed all 70 projection tests, then stopped because Hugo was absent from the default PATH.
- The same command was rerun with the previously checksum-verified official Hugo Extended 0.165.0 macOS arm64 binary through the documented HUGO_BIN setting; the strict build and validation passed.

0.3.3 version checks

- pnpm --dir apps/desktop-tauri run test:release-version
  Result: 3 passed, 0 failed.
- pnpm --dir apps/desktop-tauri run test:update-manifest
  Result: 10 passed, 0 failed.
- node apps/desktop-tauri/scripts/verify-release-version.mjs --tag yourbuddy-v0.3.3
  Result: verify-release-version: yourbuddy-v0.3.3.
- cargo check --locked --manifest-path apps/desktop-tauri/src-tauri/Cargo.toml
  Result: exit 0 with pre-existing platform/dead-code warnings; no error.

Final release preparation

- git status --short --branch
  Result before preparation: clean; branch two commits ahead of origin/master.
- pnpm --dir apps/desktop-tauri run prepare:release
  Result: compatible product sources and frozen lockfile ready for review and commit.
- Product versions: DSH 0.1.2-rc.1; Harbor Evolution 0.9.2; Codex Auth 0.3.2; Better Sidebar 0.18.0; Context Doctor 0.7.2; Plugin Marketplace 0.3.1.
- Upgrade result: no compatible DSH or product-plugin source update produced a tracked change.
- Offline Store: 587 packages installed from the frozen lock; 54 product runtime peer links resolved to the bundled workspace.
- Product smoke: 6 assembled Client plugins, external links, Plugin Marketplace, Network proxy, and Application lifecycle controls passed.
- In-progress warnings: npm downloads were slow. @openai/codex and @anthropic-ai/claude-agent-sdk-darwin-arm64 each returned pnpm download error 23 once; bounded retries succeeded.
- Final status: the complete second 0.3.3 preparation exited 0. Generated bundled resources remain ignored and are not release-source changes.

Pull-request release-blocker recovery

- Initial pull-request run: GitHub Actions run 34042773360.
- Packaged Python Runtime failure: node24-linux-x64, node24-linux-arm64, node24-macos-arm64, and node24-win-x64 stopped at profile fallback discovery with ENOENT for /yourbuddy/node_modules/react/package.json.
- Cause: pkg exposed an optional peer candidate path in its virtual filesystem without embedding the package.json bytes. The traversal recorded the candidate before proving the manifest readable.
- Fix: read each resolved dependency manifest before recording it; skip only ENOENT at the optional dependency read and preserve all other errors.
- Windows snapshot recovery: pwsh-tool-turn and persistent-pwsh-tool-turn now include the current permission preset, sandbox mode, approval policy, and runtime-context snapshot observed by Windows CI.
- ACP snapshot recovery: seven expected stdout streams now retain the current model config_option_update emitted during ACP session setup.

Focused release-blocker checks

- pnpm exec vitest run --coverage --coverage.include=packages/boot/app-boot/src/profile.ts packages/boot/app-boot/tests/profile.spec.ts
  Result: 40 passed; 100% statements, branches, functions, and lines for profile.ts.
- pnpm exec vitest run --config vitest.snapshot.config.ts snapshots/acp/acp.snapshot.ts
  Result: 15 passed.
- Complete keyless refresh/replay evidence
  Result: 113 passed, 2 skipped. The two skipped scenarios require pwsh, which is absent on this macOS host; their expected logs came from the Windows CI diff and remain pending the new Windows run.
- pnpm run doc-sync
  Result: 32 passed, 0 failed, 0 skipped.
- pnpm run lint
  Result: exit 0.

Release-shaped Python Runtime smoke

- DSH_BUILD_CLIENT_PROFILE=official pnpm exec tsx scripts/build-exe-for-python-sdk.ts --targets=node24-macos-arm64
  Result: generated Node 24.20.0 macOS arm64 executable (249.6 MB), rg helper (4.3 MB), and spawn helper.
- python3 scripts/build-python-release.py --package sdk --output-dir dist-python
  Result: deepseek_harness_sdk-0.1.2rc1-py3-none-any.whl.
- python3 scripts/build-python-release.py --package runtime --platform macos-arm64 --runtime-exe <generated executable> --output-dir dist-python
  Result: deepseek_harness_runtime_bin-0.1.2rc1-py3-none-macosx_14_0_arm64.whl (71 MB).
- Clean virtual-environment installation of both wheels
  Result: installed with runtime and SDK dependencies resolved.
- smoke-python-runtime.py --scenario all --installed-wheel
  Result: smoke-python-runtime: all passed.

Local failure and recovery boundaries

- A first full snapshot run was launched concurrently with coverage and inherited this terminal's proxy variables. Undici experimental-proxy warnings polluted subprocess stderr, and shared execution produced non-product mismatches. A serial run with the test process's terminal proxy variables cleared passed the Headless suite; the focused ACP replay passed after refreshing its seven expected outputs.
- The pkg builder deliberately deployed a production/hoisted dependency closure. The first subsequent doc-sync and lint invocations stopped before their gates because pnpm refused to purge that layout without a TTY. CI=true pnpm install --frozen-lockfile restored the development dependency layout; doc-sync and lint then passed.
- Local packaged execution covers macOS arm64 only. Linux and Windows packaged runtimes plus real pwsh execution remain pending the new pull-request CI run.

Second pull-request run and PowerShell recovery

- GitHub Actions run 34044513110 built and installed the Runtime wheels on Linux x64, Linux arm64, macOS arm64, and Windows x64. Every carrier reached `smoke-python-runtime: all passed` before entering a paid-provider preflight.
- The carrier workflow omitted the official-repository condition already owned by the real-API design. In this independent YourBuddy repository, the optional `DEEPSEEK_API_KEY_EXTERNAL` secret was empty and the four otherwise-passing carrier jobs failed at that preflight.
- Commit 7fbbd8223ad2225a27ba029d982dbf37cce4acbe restricts both preflight and live provider steps to `deepseek-harness/deepseek-harness`; that official repository still fails loud if its expected secret is absent. Other repositories continue to run the complete keyless carrier smoke without requiring a billable secret.
- Homebrew PowerShell 7.6.5 was installed on the local macOS host. A focused refresh and a separate replay of `pwsh-tool-turn|persistent-pwsh-tool-turn`, with terminal proxy variables removed from the test process, each reported 2 passed and 113 skipped by the explicit filter.
- The first focused command placed its test filter after an extra argument separator, so it launched all snapshot scenarios and generated unrelated fixture edits before stderr assertions failed on inherited proxy warnings. Those known generated changes were restored individually; only the two focused PowerShell scenario files remain in commit 7fbbd8223a.
- `pnpm run doc-sync` initially rejected the intentionally changed bilingual Agent Note until its reviewed pair record was refreshed. The second complete run reported 32 passed, 0 failed, and 0 skipped.
- Pull-request run 34044513110 also had an unrelated Coverage timing failure in `terminal-bash` (`stdin_read` expected, `inferred_idle` observed) after 16,397 tests passed. It is retained as a failed result; the next run determines whether the nondeterministic symptom repeats.

Publication boundary

- No 0.3.3 tag, GitHub Release, installer, updater channel, verification ZIP, or website deployment had been created when this record was written.
- Final public artifacts, their hashes, installed behavior, website deployment, and live download journey require separate post-publication evidence.
