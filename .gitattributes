# The repo's canonical text form is LF, enforced at checkout too: no smudge
# boundary between working tree and repo, so byte-level gates (verify-*
# comparisons, blob hashing, coverage offsets) see one form on every host.
# Windows command scripts require CRLF in the working tree for cmd.exe; the
# override after the default keeps the in-repo form normalized as text.
* text=auto eol=lf
*.cmd text eol=crlf

*.pdf binary

# External product plugins are byte-identical upstream snapshots. Keep their
# original line endings so a clean checkout reproduces the reviewed tree hash.
apps/desktop-tauri/product/context-doctor/** -text -whitespace
apps/desktop-tauri/product/dsh-better-sidebar/** -text -whitespace
apps/desktop-tauri/product/dsh-codex-auth/** -text -whitespace
apps/desktop-tauri/product/harbor-evolution/** -text -whitespace
apps/desktop-tauri/product/harbor-python/** -text -whitespace
apps/desktop-tauri/product/oil-creator/** -text -whitespace
apps/desktop-tauri/product/plugin-marketplace/** -text -whitespace
# Patch payloads preserve upstream bytes, including generated bundle whitespace.
apps/desktop-tauri/product/patches/** -text -whitespace

# The persistence catalog is the readable projection of these generated schemas.
docs/persistence-schema.json linguist-generated=true
docs/persistence-changes/*.schema.json linguist-generated=true
docs/persistence-changes/releases/*.schema.json linguist-generated=true
docs/persistence-changes/historical-formats/*.schema.json linguist-generated=true
