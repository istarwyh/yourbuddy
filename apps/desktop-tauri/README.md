# YourBuddy Desktop

English | [中文](README.zh.md)

This Tauri application embeds the existing `dsh web` client and adds the YourBuddy product assembly. The installer carries the trimmed Harness source, Harbor Evolution and its Skill, Codex Auth, the native Codex subagent provider, Better Sidebar, Context Doctor, Plugin Marketplace, Personal Workbench branding, portable CPython 3.12, and the Harbor adapter.

## Runtime layout

| Resource | Runtime behavior |
|---|---|
| `harness-source` | Carries the built source, frozen lockfile, and one compressed offline pnpm store; copied to a content-hashed application-data directory |
| `toolchain` | Checksum-pinned macOS arm64 Node 22.19.0 and pnpm 11.7.0 archives used when no compatible Host Node exists |
| `yourbuddy-runtime` | Executed from the signed application resources; supplies `harbor` and `harbor-dsh` |
| `desktop-overlay` | Selects the Codex Agent Preset and registers notifications, the Codex subagent provider, Codex Auth/Search/Image, Better Sidebar, Context Doctor, Plugin Marketplace, Personal Workbench, and Harbor Evolution through `dsh web --patch` |
| `YourBuddy/dsh-home` | Isolated sessions, settings, credentials, and web profile |
| `YourBuddy/workspace` | Fallback Harbor Workbench root when no standard profile instance is active |

The product sources live in `product/harbor-evolution`, `product/harbor-python`, `product/dsh-codex-auth`, `product/dsh-better-sidebar`, `product/context-doctor`, `product/plugin-marketplace`, and `product/personal-workbench`. `scripts/bundle-harness-source.mjs` places the six product Cordis packages and Harbor Skill in the trimmed workspace and adds them plus the in-tree `@deepseek-ai/dsh-subagent-codex` package to the CLI dependency closure, so the Host resolves every default plugin without a registry install. The generated bundle manifest records the Codex subagent package version and the product's default Agent Preset separately from the externally refreshed product snapshots. Codex Auth, Better Sidebar, and Plugin Marketplace are pinned to reviewed npm tarballs. Harbor's JavaScript and Python snapshots come from one reviewed GitHub Release, while Context Doctor has no npm release and is pinned to a reviewed GitHub `main` commit. Each externally refreshed snapshot records its immutable source, integrity hash, committed tree hash, upstream license, exact-version peer-metadata overrides, and reviewed functional compatibility patches in `YOURBUDDY_UPSTREAM.json`. `scripts/prepare-yourbuddy-runtime.mjs` installs the committed Python snapshot into a relocatable resource and accepts `YOURBUDDY_HARBOR_PYTHON_SOURCE` for temporary local adapter testing.

The bundle generator derives a system Agent Preset with id `codex` and display name **Codex** from the shipped Standard composition, enabling its existing `subagent_codex` row while leaving the other shipped presets unchanged. The desktop overlay selects Codex as the base default, so a fresh Profile and any new Session with no explicit selection can delegate immediately; a user's explicit default remains authoritative, and existing Sessions retain the composition recorded when they started. Loading the provider starts no Codex process. A delegation starts the package-local official Codex runtime in the Session workspace and uses native Codex configuration and login state rather than credentials copied into YourBuddy settings.

Every Agent-facing Harbor Tool resolves its root from the calling session's absolute working directory. Selecting `/Users/me/project` in a YourBuddy session therefore keeps initialization and later Agent-created Harbor artifacts inside that project. The desktop overlay's configured application-data `projectRoot` remains only the global Web Workbench and non-Agent fallback; Agent Tools neither use it nor reject a session because it differs.

Harbor resolves and freezes the Candidate model through the Host `agentDefaultModel` and LLM services before it starts a Job. A loopback-only Host broker then exposes that exact route to the Docker task through a random Job-scoped URL and bearer capability. The Python adapter performs an in-container health check before installing or starting the Candidate, generates an ephemeral Cordis overlay under `.harbor-runtime`, and replaces only the Candidate ACP model route with `yourbuddy-host/<frozen-model>`. The original Candidate remains immutable, and the Codex OAuth credential never enters its files, configuration, or container environment. The model binding is part of Evaluation Context v2, so a different provider, model, reasoning effort, transport, or protocol cannot reuse an old comparison baseline.

Host Node reuse requires both the supported version and the native CPU architecture. Global pnpm installations are never adopted; YourBuddy provisions its own pinned pnpm to avoid wrappers and native packages installed by a Node build for another architecture. On a cold start, every archive digest is checked, the dependency store is expanded, and `pnpm install --prod --frozen-lockfile --offline` reconstructs `node_modules`; the temporary expanded store and copied archive are removed after success. The 35,000-file Store is compressed to one application resource to reduce the installed app and avoid platform-link failures during Tauri packaging. The macOS build runs App/updater, DMG, and App/updater bundling as separate stages so Finder DMG decoration cannot discard the signed updater artifacts.

The application prepends its private `dsh`, Node, and pnpm shims only to the Host process tree. It does not overwrite a user's global `dsh` command or shell profile unless a developer explicitly launches it with `YOURBUDDY_PERSIST_DSH_CLI=1`. Product plugin overlays reuse an active standard Profile Bundle by package name and enable a uniquely named in-box fallback only when no prior mount exists, so a user-installed Codex subagent, Harbor, Codex Auth, Better Sidebar, Context Doctor, Plugin Marketplace, or Personal Workbench bundle does not create a duplicate Loader ID. During an upgrade, the native boot path also rebinds only YourBuddy-managed `link:` dependencies that still point into this application's older content-addressed Harness trees and runs the standard Profile install; registry dependencies and links outside the YourBuddy application-data tree remain user-owned and unchanged.

Plugin Marketplace uses the public GitHub `dsh-plugin` topic only for discovery. Opening a result searches npm and enables one-click confirmation only when a package declares `dsh.bundle.patch` and its metadata links it to that repository through the Repository field or owner-scoped DSH upstream metadata; this selects a DSH Bundle from repositories that publish SDK, CLI, and other npm packages and resolves scoped packages whose npm name differs from the repository basename. Missing, ambiguous, or incomplete metadata keeps confirmation disabled. A confirmed install runs `dsh plugin add` against the isolated Web Profile, keeps its progress or actionable pnpm failure associated with that package, and grants installed code the same Host privileges as a manual DSH plugin install. Repository and npm links use a fixed iframe message protocol whose shell and Rust validators allow only an HTTPS GitHub repository, npm search, or npm package page before the system browser opens it.

The desktop shell launches its private Host with `dsh web --no-open`. The loopback URL remains the internal transport loaded by the Tauri WebView, but startup never hands that URL to the operating system's default browser.

Assistant Markdown continues to use the shared renderer's HTTP(S) allowlist. In the desktop product, the Personal Workbench Client intercepts only its external `_blank` anchors and asks the parent shell to open them in the operating system's default browser. Hover exposes the destination, and the link context menu can open or copy it. The shell accepts the fixed versioned request only from the active Host iframe at its exact origin; both the shell and Rust command require a bounded, credential-free HTTP(S) URL. Relative links, same-origin routes, downloads, file mentions, and schemes such as `javascript:`, `file:`, and `data:` never enter the native opener.

Release builds read the semantic application version embedded by Tauri and run one signed update check at a time after the main window opens. The stable `yourbuddy-updater` manifest must advertise a higher version before YourBuddy downloads anything; the check has a 15-second deadline, while a failure leaves the running workbench unchanged. The tray shows the current version and supports a manual check. **Settings → General → Application lifecycle** exposes **Check and update** for the same signed flow and **Restart YourBuddy** for loading newly installed plugins. The loopback Client can send only the fixed versioned `check-update` and `restart` requests; the Tauri shell accepts them only from the active workbench iframe at the exact Host origin and maps them to literal allowlisted commands. An update announces the current and target versions before Tauri verifies and installs the signed artifact. A manual restart and a successful update both stop the private Host before relaunching the application. These user actions never run local release preparation or download plugin source: bundled product plugins advance only as part of the signed YourBuddy release.

**Settings → General → Network proxy** owns one application-wide policy rather than a Codex-specific transport toggle. Direct mode removes ambient proxy variables; system mode reads fixed macOS HTTP and HTTPS endpoints with `/usr/sbin/scutil`; custom mode requires separate credential-free HTTP and HTTPS URLs and accepts additional bypass hosts. PAC, automatic proxy discovery, and HTTP-only macOS configurations fail with an actionable message because they cannot be translated faithfully to the complete Node process tree. Native reqwest clients and the signed updater use rustls with the platform verifier, while every application-owned Node process receives `--use-system-ca`. A user can also select one PEM-encoded `.pem` or `.crt` enterprise CA bundle through the native file picker; YourBuddy validates and canonicalizes the file, stores its path, adds its roots to native platform trust, and sets `NODE_EXTRA_CA_CERTS` before spawning the Node Host and plugin processes. The explicit bundle supplements system trust and never disables certificate validation. The test checks the desktop draft route and the running Node Host global `fetch` separately and labels their HTTP or bounded certificate and transport results, proxy mode, and CA source independently, so one successful route cannot mask the other's failure; when the draft differs from the active Host policy, the result asks for a save, restart, and second test. Saving invokes only the fixed network-settings command and then the fixed application restart command; the restart terminates and waits for the private Host before relaunch, after which the Host, plugins, package installation, runtime provisioning, and signed updater all use the same resolved policy. Loopback addresses always bypass the proxy, and running processes keep their previously activated policy until restart.

The desktop overlay injects a Content Security Policy and no-referrer policy into the served workbench. The Tauri-owned splash and shell also have a CSP; the workbench keeps `unsafe-eval` because Cordis client plugins are evaluated dynamically, while objects and base-URL mutation remain disabled.

## Local release preparation

Run the networked product refresh explicitly before reviewing and committing a release input:

```sh
pnpm --dir apps/desktop-tauri run prepare:release
```

The DSH policy selects the highest official stable Release and falls back to the highest RC only while no stable Release exists; alpha, beta, other prereleases, and `master` are excluded. When the selected tag is newer, release preparation requires a clean worktree, fetches and verifies that exact tag commit, leaves its upstream merge uncommitted for review, records it in `product/DSH_UPSTREAM.json`, and retargets the approved product peer metadata before compatibility checks. The remaining refresh policy queries npm's latest stable versions for Codex Auth, Better Sidebar, and Plugin Marketplace, the latest stable Harbor GitHub Release for the paired Cordis plugin and Python adapter, and the Context Doctor `main` head. Personal Workbench is first-party and receives only the selected DSH peer version. GitHub API reads prefer `GITHUB_TOKEN` or `GH_TOKEN`, then reuse an existing `gh auth` login when available; the credential is never written to provenance. The command does not search backward for an older compatible release: an incompatible selected candidate fails so its policy or code can be reviewed. A peer-metadata override is accepted only when `product/plugin-update-policy.json` names the exact upstream version; a functional compatibility patch remains explicit in provenance and must continue to pass the assembled release smoke.

The policy is validated before any managed path is copied or removed: destinations must remain under `product/`, source paths must remain under the downloaded checkout, and managed paths must be unique and non-overlapping. All archives use HTTPS, compressed and expanded byte limits, entry-count limits, and safe extraction; npm artifacts must match the registry SRI, and every archive receives digest-bound provenance. Candidates remain in staging until the complete set passes package identity, managed Node, DSH peer, bundle patch, Host entry, Client entry and injection, license, and tree checks. The command then replaces the managed snapshots as one operation, rebuilds Harness with the same explicit Client environment as tagged CI, regenerates `product/harness-pnpm-lock.yaml`, and proves every product DSH/Cordis peer in both the lockfile and installed tree points to the bundled workspace instead of a second runtime. It prepares the digest-bound offline Store and performs the real `--prod --frozen-lockfile --offline` install. Release acceptance and native first launch disable pnpm's automatic version switch and trust the committed lockfile because the product independently pins the pnpm archive and verifies the Store and bundle digests. The Harbor commands and assembled Host run with a credential-scrubbed environment plus temporary home, DSH, and temporary directories. Headless Chromium must receive all six product Client bundles, activate every Client Loader entry without page or console errors, mount the workbench, show Codex as the default Agent Preset, expose the Plugin Marketplace navigation entry, exercise a safe assistant-link DOM contract through hover, right-click copy, and the system-browser command, exercise the Network proxy draft test and save-and-restart controls, expose the Application lifecycle update and restart controls, reject one-click installation when the repository has only a non-DSH npm package, select a differently named scoped DSH Bundle while ignoring a sibling SDK package, invoke only the restricted general and Marketplace external-link, network-settings, signed-update, and application-restart commands, enable installation confirmation, and render a package-specific persisted failure; the same smoke requests the Context Doctor API and requires the Host to shut down normally. A failure at any later stage restores every managed snapshot and the product lockfile.

Managed product paths must be clean by default. `pnpm --dir apps/desktop-tauri run prepare:release -- --allow-dirty` is an explicit escape when the developer has already reviewed the local product edits that the transaction will preserve or replace; it never permits an upstream DSH merge in a dirty worktree. A successful run leaves the DSH merge, refreshed snapshots, and lockfile for human review and commit; `DSH_UPSTREAM.json` records the official DSH tag and commit, each `YOURBUDDY_UPSTREAM.json` records the exact external revision plus archive and tree hashes, and the generated `.bundle-manifest.json` records these inputs plus the whole-bundle hash. Any later preparation failure aborts an owned DSH merge and restores the managed product inputs.

Tagged CI and ordinary `prepare:dist` or `build` commands never mutate upstream inputs. A newly pushed tag resolves the DSH policy and refuses to publish when the committed source, provenance, or ancestry is stale. Manual workflow dispatch for an existing tag skips that live freshness check and consumes only the tag's committed snapshots and frozen lockfile, so an old release remains reproducible after later npm or GitHub changes.

## Commands

Run from the repository root:

```sh
pnpm install --frozen-lockfile
DSH_CLIENT_TITLE=YourBuddy pnpm run build
pnpm --dir apps/desktop-tauri run test:bundle
pnpm --dir apps/desktop-tauri run test:offline
pnpm --dir apps/desktop-tauri run test:overlay
pnpm --dir apps/desktop-tauri run test:update-manifest
pnpm --dir apps/desktop-tauri run test:release-version
pnpm --dir apps/desktop-tauri run sync:dsh -- --dry-run
pnpm --dir apps/desktop-tauri run prepare:release
pnpm --dir apps/desktop-tauri run prepare:product-runtime
pnpm --dir apps/desktop-tauri run build
```

The current target is `aarch64-apple-darwin`; the release workflow intentionally has no Windows, Intel macOS, or Linux matrix rows.

The macOS arm64 release gate verifies the tag against every desktop version source and, for a new tag, requires the committed DSH Release selected by policy. It builds the committed Harness and signed-update artifacts without refreshing product-plugin channels, then runs a focused Host startup-argument contract across the DSH parser plus native and WSL desktop launchers. It relocates the portable runtime out of the application bundle, breaks the original Python-home reference deliberately, and requires both `harbor --version` and `harbor-dsh --help` to succeed before checksumming and publishing the artifacts. The gate stays intentionally narrower than the repository's full test matrix so a desktop patch release does not wait on unrelated platforms or packages.
