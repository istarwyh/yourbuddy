- id: skill-badge
  disabled: false

- id: tool-skill
  config:
    modelExcludedSkills:
      - dsh-badge

- id: skill-filesystem
  config:
    watch: false

- id: session-telemetry-otel
  disabled: true
